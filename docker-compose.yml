services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    working_dir: /app
    volumes:
      - .:/app
    env_file:
      - .env

networks:
  default:
    name: scnet